<div class="container">
  <%= simple_form_for [@list] do |l| %>
  <%= l.input :name %>
  <%= l.input :photo, as: :file %>
  <%= l.submit class: 'btn btn-secondary' %>
  <% end %>
  <%= link_to 'Back to Lists', lists_path, class: 'btn btn-secondary mt-2' %>
</div>
